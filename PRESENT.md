# ❄️ SnowCup - 世界の降雪情報リアルタイム可視化アプリ

> C3 Snow Cup 発表資料

---

## 1. プロダクト概要

### 何を作ったか

**SnowCup** は、世界中の任意の地点における降雪情報をリアルタイムで取得・可視化する Web アプリです。

地名を検索するだけで、その地点の現在の積雪・降雪量・気温がわかり、過去 7 日〜今後 7 日間の推移をグラフとマップで確認できます。

### ターゲット

- スキー・スノーボードに行く前に雪情報を確認したい人
- 世界各地の降雪スポット（ニセコ、白馬、アルプスなど）を横断的に比較したい人

---

## 2. デモ

### プリセット地点（すぐに確認できる国内スキーエリア）

| 地名 | 場所 |
|------|------|
| 札幌 | 北海道 |
| ニセコ | 北海道 |
| 白馬 | 長野県 |
| 蔵王 | 宮城県 |

### 主な操作フロー

```
① 地名を検索 or プリセット地点を選択
② 現在の気象データを確認（気温・降雪量・積雪深）
③ マップ上のマーカーで地点を確認
④ グラフで過去 7 日〜今後 7 日の降雪推移を確認
⑤ 5 分ごとに自動更新
```

---

## 3. 機能一覧

| 機能 | 説明 |
|------|------|
| 🔍 場所検索 | 世界中の地名を日本語・英語で検索、候補をドロップダウン表示 |
| 🌡️ 現在の気象表示 | 気温・降雪量・積雪深をリアルタイムで表示 |
| 📊 降雪グラフ | 過去 7 日 + 予測 7 日の時系列グラフで積雪量を可視化 |
| 🗺️ マップ表示 | インタラクティブマップ上に気象情報マーカーを配置 |
| ⚡ 自動更新 | 5 分ごとにデータを自動リフレッシュ |
| 📍 プリセット地点 | 札幌・ニセコ・白馬・蔵王をワンクリックで確認 |

---

## 4. 技術スタック

```
┌─────────────────┐
│   ブラウザ       │
│   (Next.js UI)   │
└────────┬────────┘
         │ HTTP/REST
         ▼
┌──────────────────────────────┐
│  Go バックエンド (API プロキシ) │
│  - キャッシュ層 (MySQL)        │
│  - レート制限・CORS            │
│  - データ変換・整形            │
└────┬──────────────────┬──────┘
     │ SQL              │ HTTPS
     ▼                  ▼
┌─────────────┐  ┌─────────────────────┐
│  MySQL DB   │  │  Open-Meteo API     │
│ (キャッシュ) │  │  (無料・APIキー不要)  │
└─────────────┘  └─────────────────────┘
```

### フロントエンド

- **Next.js 14** (App Router) + **TypeScript**
- **Leaflet** - インタラクティブマップ
- **Chart.js** - 降雪量グラフ
- **CSS Modules** - スコープ付きスタイリング
- カスタムフック: `useWeatherData` / `useGeocoding` / `useMap`

### バックエンド

- **Go** + **Gin** フレームワーク
- クリーンアーキテクチャ: Handler → Service → Repository
- ミドルウェア: CORS / レート制限 / アクセスログ

### データベース

- **MySQL** - 気象データのキャッシュ（TTL: 1〜3 時間）

### インフラ

- **Docker + Docker Compose** - 全サービスをコンテナ化

---

## 5. 外部 API

### Open-Meteo API

- **完全無料** で商用利用可能、**API キー不要**
- 1 日 10,000 リクエストまで対応

| API | 用途 |
|-----|------|
| Geocoding API | 地名 → 緯度経度 変換 |
| Weather Forecast API | 現在 + 過去 + 予測の気象データ取得 |

取得できる気象データ:
- `temperature_2m` - 気温 (℃)
- `snowfall` - 1 時間あたりの降雪量 (cm)
- `snow_depth` - 積雪深 (m)
- `weather_code` - 天気コード (WMO 準拠)

---

## 6. アーキテクチャのこだわり

### ① バックエンドプロキシ構成

フロントエンドから Open-Meteo API に**直接アクセスさせない**設計。

- 外部 API の URL・仕様がフロントエンドに露出しない
- 将来の API 移行時にフロントエンドの変更が不要
- バックエンドで一元的にバリデーション・エラーハンドリング

### ② MySQL キャッシュ層

同一地点への連続リクエストを MySQL で吸収。

- API レート制限 (10,000 req/日) の消費を抑制
- レスポンス速度の向上
- TTL 管理で鮮度を保証（現在データは短め / 時系列データは長め）

### ③ セキュリティ

| 対策 | 内容 |
|------|------|
| CORS | 許可オリジンをフロントエンドのみに限定 |
| レート制限 | IP ベースで過剰リクエストを遮断 |
| 入力バリデーション | 緯度経度の範囲チェック、SQLインジェクション対策 |
| エラー隠蔽 | スタックトレース等の詳細情報を外部に返さない |

---

## 7. ディレクトリ構成

```
snow_cup/
├── backend/              # Go バックエンド
│   ├── cmd/server/       # エントリーポイント
│   ├── internal/
│   │   ├── handler/      # REST API ハンドラー
│   │   ├── service/      # ビジネスロジック
│   │   ├── repository/   # DB アクセス層
│   │   ├── middleware/   # CORS・レート制限・ログ
│   │   └── model/        # データモデル
│   └── migrations/       # DB マイグレーション
└── frontend/             # Next.js フロントエンド
    └── src/
        ├── app/          # App Router ページ
        ├── components/   # UI コンポーネント
        ├── hooks/        # カスタムフック
        ├── lib/          # API クライアント
        └── types/        # TypeScript 型定義
```

---

## 8. API エンドポイント

| Method | Path | 説明 |
|--------|------|------|
| `GET` | `/api/weather?lat={lat}&lon={lon}` | 気象データ取得（キャッシュ優先） |
| `GET` | `/api/geocoding?name={地名}` | 場所検索（候補最大 10 件） |
| `GET` | `/health` | ヘルスチェック |

---

## 9. 苦労した点・工夫した点

### キャッシュ設計

- DB 接続なしでもサービスが正常動作するよう設計（キャッシュは optional）
- キャッシュ読み書きエラーはログ出力のみで処理を継続し、可用性を優先

### Next.js の SSR と Leaflet の共存

- Leaflet はブラウザ専用のため、動的インポート (`dynamic import`) で SSR を回避
- `MapWrapper.tsx` でクライアントサイドのみのレンダリングを担保

### 自動更新とユーザー体験

- 5 分間隔の自動更新でウィンドウフォーカス時のデータ鮮度を確保
- ローディング中もスピナーを表示して UX を損なわない設計

---

## 10. 今後の展望

- [ ] 複数地点の同時比較表示
- [ ] 降雪アラート通知機能
- [ ] 過去の積雪データとの比較（今年 vs 例年）
- [ ] PWA 対応でスマートフォン最適化
- [ ] スキー場ランキング機能（積雪量/降雪量）
- [ ] バックグラウンドで人気地点のキャッシュを自動 warm-up

---

## 11. まとめ

| 項目 | 内容 |
|------|------|
| プロダクト名 | SnowCup |
| コンセプト | 世界の降雪情報をリアルタイムで、誰でも簡単に |
| 技術 | Go / Next.js / MySQL / Docker |
| 外部 API | Open-Meteo（無料・キー不要） |
| 特徴 | プロキシ + キャッシュ構成 / セキュリティ重視 / 自動更新 |

> 「どこのゲレンデが今一番雪が積もってるか、すぐにわかる」

---

*発表者: C3 Snow Cup チーム*
